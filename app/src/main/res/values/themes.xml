<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Themuvidb" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue200</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue200</item>
        <item name="colorSecondaryVariant">@color/blue400</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:itemBackground">@color/black</item>
        <item name="android:headerBackground">@color/blue900</item>
    </style>

    <style name="App.TextView" parent="Widget.MaterialComponents.TextView">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="App.TextView.Sub" parent="Widget.MaterialComponents.TextView">
        <item name="android:textColor">@color/blue400</item>
    </style>
    <style name="App.TextView.Sub2" parent="Widget.MaterialComponents.TextView">
        <item name="android:textColor">@color/gray200</item>
    </style>
    <style name="CollapsingToolbarLayoutExpandedTextStyle" parent="Theme.Themuvidb">
        <item name="android:textColor">@android:color/transparent</item>
        <item name="android:textSize">0sp</item>
    </style>
    <style name="RatingBar" parent="Theme.Themuvidb">
        <item name="colorControlNormal">@color/gray200</item>
        <item name="colorControlActivated">@color/yelow</item>
    </style>
    <style name="RatingBar.Small" parent="Widget.AppCompat.RatingBar.Small">
        <item name="colorControlNormal">@color/gray200</item>
        <item name="colorControlActivated">@color/yelow</item>
    </style>
</resources>